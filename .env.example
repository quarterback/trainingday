# Postgres connection string. Works with Vercel Postgres, Supabase, Neon, or local.
DATABASE_URL=postgres://user:password@host:5432/dbname

# Optional: Literal handle for `npm run import:literal`. Defaults to "ron".
LITERAL_HANDLE=ron
