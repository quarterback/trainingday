import type { NewStory } from "@/lib/schema";

// Stories are intentionally seeded empty — per the spec, the user writes
// these by hand because the act of writing is the learning. Add yours via
// the New Entry button or by editing this file and re-running db:seed.
export const seedStories: NewStory[] = [];
